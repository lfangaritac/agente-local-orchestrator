# Handoff Package

- run_id: `20260508_000001_c0ffee12`
- project_id: `orchestrator`
- source_agent: `continue`
- target_agent: `builder`
- scenario: `implementation`
- risk_level: `medium`
- information_volume: `high`
- preflight_status: `unavailable_timeout`

## Objective

Implementar la herramienta MCP `create_and_dispatch_opencode_handoff` y su script soporte para eliminar la manualidad del usuario transportando handoffs entre Continue y OpenCode.

Nota: el preflight MCP `orchestrator_preflight` no estuvo disponible por timeout (-32001). No inventar campos; operar con restricciones explícitas del usuario y registrar la limitación.

## Context Sources

- `AGENT_ORCHESTRATION.md`
- `MODEL_ROUTING.md`
- `docs/protocols/AGENT_AUTOMATION_PROTOCOL.md`
- `CONTINUE_USAGE_PROTOCOL.md`
- `docs/alerts/GLOBAL_CRITICAL_ALERTS.md`
- `docs/lessons/GLOBAL_LESSONS_LEARNED.md`

## Allowed Files (scope)

- `scripts/create_and_dispatch_opencode_handoff.py`
- `mcp_server/schemas.py`
- `mcp_server/tools.py`
- `mcp_server/README.md`
- `mcp_server/test_create_and_dispatch_opencode_handoff_stdio.py`

## Authorization / Restrictions

Build autorizado dentro del alcance. Permitido: crear/modificar archivos en scope, ejecutar validaciones locales no destructivas, registrar evidencia.

Prohibido: premium/Replit, secrets, deployment, migraciones, acciones destructivas, mover/borrar/renombrar archivos maestros, push/merge.

## Implementation Contract (prompt for OpenCode)

Implementa en este repo la herramienta MCP **create_and_dispatch_opencode_handoff** y su script soporte para eliminar la manualidad del usuario transportando handoffs.

Requisitos:
- Crear `scripts/create_and_dispatch_opencode_handoff.py` (o extender `scripts/start_opencode_from_handoff_async.py` sin romper compatibilidad).
- Crear herramienta MCP `create_and_dispatch_opencode_handoff` y registrarla en:
  - `mcp_server/schemas.py`
  - `mcp_server/tools.py`
  - `mcp_server/README.md`
- Crear prueba:
  - `mcp_server/test_create_and_dispatch_opencode_handoff_stdio.py`

Funcionalidad:
- Input mínimo: `project_id`, `objective`, `handoff_body`, `target_agent`, `model`, `risk_level`, `scenario`, `allowed_files`, `validation_commands`, `requires_authorization`, `authorization_granted`
- Debe escribir handoff en `docs/agent_queue/inbox` (json + md), crear/reutilizar run_id, crear `TRACE.md` y `RUN_SUMMARY.md` iniciales en `docs/agent_runs/<run_id>/`.
- Autorización:
  - Si `requires_authorization=true` y `authorization_granted=false`: NO despachar OpenCode; devolver `status=waiting_authorization`.
  - Si `authorization_granted=true` (o `requires_authorization=false`): despachar OpenCode async; devolver `status=dispatched`.
- Output obligatorio: `ok`, `status`, `run_id`, `handoff_json_path`, `handoff_md_path`, `run_dir`, `trace_path`, `summary_path`, `background_meta_path` (si aplica), `target_agent`, `model`, `next_tool=check_opencode_run_status`, `user_message` (amigable).

Validaciones obligatorias a ejecutar y reportar:
- `python -m py_compile .\\scripts\\create_and_dispatch_opencode_handoff.py`
- `python -m py_compile .\\mcp_server\\schemas.py`
- `python -m py_compile .\\mcp_server\\tools.py`
- `python -m py_compile .\\mcp_server\\server.py`
- `python -m py_compile .\\mcp_server\\test_create_and_dispatch_opencode_handoff_stdio.py`
- `python .\\mcp_server\\test_create_and_dispatch_opencode_handoff_stdio.py`

La prueba stdio debe validar:
- `initialize_ok=true`
- `tools_list_ok=true`
- `create_and_dispatch_opencode_handoff` aparece en `tools/list`
- `tools/call` funciona
- `authorization_granted=false` devuelve `waiting_authorization` y NO despacha OpenCode
- `authorization_granted=true` devuelve `dispatched` (o estado equivalente válido)
- se crean `handoff_json` + `handoff_md` + `TRACE.md` + `RUN_SUMMARY.md`
- se devuelve `run_id`
- se devuelve `next_tool=check_opencode_run_status`

Entrega final esperada:
1) archivos creados/modificados
2) resumen de implementación
3) resultados de pruebas
4) ejemplo de output amigable
5) riesgos residuales
6) `git status --short`
7) commit sugerido (no ejecutar): "Agregar despacho MCP de handoffs a OpenCode"

## Status

created
