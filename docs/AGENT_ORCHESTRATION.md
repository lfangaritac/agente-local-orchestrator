# AGENT_ORCHESTRATION (stub)

Este archivo **no es canónico**.

La fuente de verdad para la arquitectura / orquestación de agentes es:

- `../AGENT_ORCHESTRATION.md`

## Reglas de uso

- **No** duplicar contenido aquí.
- Cualquier actualización de arquitectura u orquestación **debe** hacerse en `AGENT_ORCHESTRATION.md` (raíz del repo).
- Este archivo existe únicamente como **referencia liviana** para navegación desde `docs/` y para compatibilidad de enlaces.

> Nota: algunas herramientas (p.ej. `verify_master_files`) pueden seguir reportando ambos paths como “duplicate candidates”. En esta iteración, eso se considera un pendiente técnico (stubs aceptados) y se resolverá en una futura actualización de semántica/validación.

